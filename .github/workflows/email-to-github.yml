name: Email to GitHub

on:
  workflow_call:
    secrets:
      IMAP_PASSWORD:
        required: true
      SMTP_PASSWORD:
        required: false
        description: "SMTP password (not used in email-to-github)"
    inputs:
      IMAP_HOST:
        required: true
        type: string
      IMAP_PORT:
        required: true
        type: string
      IMAP_USER:
        required: true
        type: string
      SMTP_HOST:
        required: false
        type: string
      SMTP_PORT:
        required: false
        type: string
      SMTP_USER:
        required: false
        type: string
      PROJECT_ID:
        required: false
        type: string

jobs:
  process-emails:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout helpdesk scripts
        uses: actions/checkout@v4
        with:
          repository: MLoacher/github-actions-helpdesk
          path: helpdesk-scripts

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: helpdesk-scripts/requirements.txt

      - name: Install dependencies
        run: |
          pip install -r helpdesk-scripts/requirements.txt

      - name: Process emails
        env:
          IMAP_HOST: ${{ inputs.IMAP_HOST }}
          IMAP_PORT: ${{ inputs.IMAP_PORT }}
          IMAP_USER: ${{ inputs.IMAP_USER }}
          IMAP_PASSWORD: ${{ secrets.IMAP_PASSWORD }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PROJECT_ID: ${{ inputs.PROJECT_ID }}
        run: |
          python helpdesk-scripts/scripts/email_to_github.py
